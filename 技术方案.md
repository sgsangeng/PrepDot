# PrepDot 技术方案

> 本文档描述项目**当前实际实现**的技术架构，随开发进度持续更新。
> 最近更新：完成统一鉴权体系重构与越权漏洞修复。

## 1. 项目定位

PrepDot 是一个 AI 辅助的间隔重复复习应用：用户上传学习资料（文本/PDF/Word/PPT），AI 自动拆解成结构化闪卡并组织成层级卡组，用户按每日计划复习，系统根据复习表现动态调整下次复习时间。

技术选型上放弃了最初设想的"本地优先桌面应用"（Next.js + Tauri + SQLite）路线，改为更贴近真实生产环境、也更利于简历展示后端能力的**前后端分离架构**：iOS 原生客户端 + Spring Boot 后端服务 + MySQL。

## 2. 总体架构

```
┌─────────────────────┐         HTTPS/JSON          ┌──────────────────────────┐
│   iOS App (SwiftUI)  │  ────────────────────────▶  │   Spring Boot 后端服务    │
│   iOS 26, @Observable│  ◀────────────────────────  │   端口 8080               │
└─────────────────────┘                              └──────────┬───────────────┘
                                                                  │
                                            ┌─────────────────────┼─────────────────────┐
                                            ▼                     ▼                     ▼
                                      ┌───────────┐       ┌──────────────┐      ┌──────────────┐
                                      │  MySQL 8   │       │  DeepSeek API │      │ PDFBox/POI   │
                                      │ MyBatis-Plus│      │ (卡片生成)    │      │ (文件解析)    │
                                      └───────────┘       └──────────────┘      └──────────────┘
```

- iOS 客户端不直接持有任何第三方 AI Key，所有 AI 调用都经由后端代理，Key 存在后端配置文件里
- 鉴权基于 JWT，客户端持久化 token，每次请求携带 `Authorization: Bearer <token>`
- 后端是无状态服务（不依赖 session），水平扩展友好

## 3. 技术栈

### 3.1 后端

| 组件 | 选型 | 版本 |
|---|---|---|
| 框架 | Spring Boot | 3.3.4 |
| ORM | MyBatis-Plus | (spring-boot3-starter) |
| 数据库 | MySQL | 8.0 |
| 鉴权 | JWT (jjwt) | 0.12.6 |
| 密码加密 | Spring Security Crypto (BCrypt) | — |
| API 文档 | Knife4j (springdoc-openapi3) | — |
| 文件解析 | Apache PDFBox / Apache POI | 3.0.2 / 5.3.0 |
| AI 服务 | DeepSeek API（HTTP 直连，`java.net.http.HttpClient`） | — |

选型理由：
- MyBatis-Plus 而非 JPA：SQL 可控性更高，适合复杂查询（层级卡组、聚合统计），Lambda 查询语法减少了硬编码字段名的出错风险
- jjwt 而非 Spring Security 全家桶：项目鉴权需求单一（只需签发/校验 token），引入完整 Spring Security 增加不必要的复杂度和学习成本；用一个轻量 Filter 手动实现鉴权链路，也更利于讲清楚底层原理
- HttpClient（JDK 11+ 内置）而非 RestTemplate/WebClient：无需额外依赖，`connectTimeout`/`timeout` 可精细控制，满足调用 AI 接口这种慢速外部依赖的场景

### 3.2 iOS 客户端

| 组件 | 选型 |
|---|---|
| UI 框架 | SwiftUI，最低 iOS 26 |
| 状态管理 | `@Observable`（非 `ObservableObject`），`@Environment` 注入 |
| 网络层 | `URLSession` 手写封装（`APIService` 单例） |
| 图谱渲染 | 自研 Fruchterman-Reingold 力导向布局算法（`Canvas` + `TimelineView` 手动绘制，无第三方图形库） |

## 4. 鉴权与安全设计

这是本项目投入最多设计思考的部分，从"每个 Controller 各自手写、部分接口完全裸奔"重构为统一的过滤器链路。

### 4.1 重构前的问题

- 鉴权逻辑散落在各 Controller，写法不一致，`@RequestHeader(required = false)` 导致未登录请求被静默放行（`userId = null`）而非拒绝
- `CardController` 的更新/删除/批量删除接口**完全没有权限校验**——任何登录用户凭 ID 即可修改或删除他人的卡片（越权访问，IDOR）
- `PlanServiceImpl.submitReview` 同样缺少归属校验，可篡改他人卡片的记忆度分数
- 排查一次"AI 生成的卡组在客户端看不到"的真实故障时定位到根因：客户端某个请求漏带 `Authorization` header，后端因为"宽松放行"策略没有拒绝，而是把这批数据写成了 `user_id = NULL` 的孤儿数据

### 4.2 重构后的架构

```
请求 → JwtAuthFilter（@Order(1)，早于业务逻辑）
         │
         ├─ 白名单路径（/api/auth/login、/register、Swagger）→ 直接放行
         │
         ├─ 无 token / token 无效 → 直接返回 401，不进入 Controller
         │
         └─ token 有效 → UserContext.set(userId) → chain.doFilter()
                              │                         │
                              │                    Controller / Service
                              │                    通过 UserContext.getUserId() 取用户身份
                              │                    归属校验下沉到 Service 层（如 CardServiceImpl）
                              ▼
                    finally { UserContext.clear() }
```

关键设计决策：

- **用 Servlet `Filter` 而非 Spring MVC `Interceptor`**：鉴权只需要 URL 和 header，不依赖 `HandlerMethod`，Filter 执行时机更早（`DispatcherServlet` 之前），无效请求不会浪费后续的参数绑定和业务逻辑
- **用 `ThreadLocal`（`UserContext`）传递当前用户，而非层层传参**：Tomcat 是一请求一线程模型，`ThreadLocal` 天然是请求级作用域；避免 `userId` 参数在 Controller → Service → Mapper 之间反复透传
- **`finally` 块中强制 `clear()`**：Tomcat 线程池复用线程，若不清理，下一个复用该线程的请求会读到上一个用户的身份（跨用户串号），这是真实存在的安全隐患，不是理论风险
- **异常体系与 HTTP 状态码解耦**：新增 `BusinessException`（携带 401/403/404/400 状态码），配合 `GlobalExceptionHandler` 统一转换，替代此前清一色的 `RuntimeException`（导致所有业务错误对前端来说都是模糊的 400）
- **越权校验下沉到 Service 层而非 Controller**：`CardServiceImpl` 内部对 create/update/delete 都做"卡片所属卡组是否属于当前用户"的校验，即使未来有新的调用方也天然带保护，不会因为某个 Controller 忘写校验又出现裸奔接口

### 4.3 IDOR（越权访问）修复清单

| 接口 | 修复前 | 修复后 |
|---|---|---|
| `PUT/DELETE /api/cards/{id}` | 无归属校验，任何登录用户可改删他人卡片 | `CardServiceImpl` 校验卡片所属卡组的 `user_id` |
| `DELETE /api/cards/batch` | 批量删除无校验 | 一次批量查询卡片→卡组归属关系（避免 N+1），只删除确认属于自己的部分，其余静默跳过并记录日志 |
| `POST /api/plan/review` | 可对任意 `cardId` 提交评分，篡改他人记忆度 | 提交前校验卡片所属卡组归属 |
| `POST /api/community/packs/{id}/import` | 导入的卡包硬编码 `userId = null` | 归属到当前登录用户 |

### 4.4 已知设计取舍

- `deck.user_id`/后续查询中允许 `userId == null` 的历史数据被任何登录用户操作，用于兼容早期未接入鉴权时产生的公共/测试数据，属于有意为之的宽松策略，不是漏洞
- 当前无角色/权限分级（RBAC），所有登录用户权限等同，仅做"资源归属"校验，不涉及"操作类型"授权——如未来引入管理员角色，会在 Filter 之后增加基于注解的方法级鉴权（`@RequireRole` + AOP/Interceptor）

## 5. 数据模型

核心表结构（MySQL，utf8mb4）：

| 表 | 说明 | 关键字段 |
|---|---|---|
| `user` | 用户 | `username`、`password`(BCrypt)、`study_target` |
| `deck` | 卡组，支持最多 3 层层级（AI 生成时会拆分为 根卡组→子卡组） | `parent_id`（自引用外键）、`depth`（0/1/2）、`user_id` |
| `flashcard` | 闪卡 | `deck_id`、`card_type`（qa/choice/blank）、`options`（选择题选项 JSON）、`memory_score`（0-100）、`next_review_at` |
| `daily_plan` | 每日复习计划，按 (`plan_date`, `user_id`) 唯一 | `total_count`、`completed_count` |
| `daily_plan_item` | 计划项，关联具体卡片 | `plan_id`、`card_id`、`status`(pending/done) |
| `review_record` | 每次复习的评分记录，用于统计和后续算法迭代 | `rating`、`memory_score_before/after` |
| `app_settings` | 全局配置（非用户级），如每日目标、AI Key | key-value 形式 |

设计要点：
- `deck` 用自引用外键 + `depth` 字段实现有限层级（最多 3 层），而非无限递归树，因为 AI 生成的卡组结构在产品上就是"大主题→小主题"两级，不需要支持任意深度
- `flashcard` 用 `card_type` + 可空的 `options` 字段承载三种卡片形态（问答/选择/填空），没有拆成多张子表，因为字段差异小、查询场景统一，拆表反而增加 join 成本

## 6. 核心业务流程

### 6.1 AI 生成卡组流程

1. 用户上传文件（txt/pdf/docx/pptx/md）或粘贴文本
2. 后端用 PDFBox/POI 提取纯文本
3. 拼接 Prompt（约束：JSON 格式、层级结构、每卡只考一个点）调用 DeepSeek API
4. AI 返回结构化 JSON（预览，不入库），客户端展示预览
5. 用户确认后调用 `/api/ai/import`，后端递归解析 JSON 建立 `deck` 层级 + 批量插入 `flashcard`

> 当前该调用是**同步阻塞**的（等待 AI 响应最长 120 秒），这部分识别为后续优化点，见第 8 节。

### 6.2 复习流程与算法

当前使用简化版间隔重复规则（非完整 SM-2 算法）：

```
初始记忆度：35
again（不会）: -18 分，立即重新出现（0 天后）
hard（困难） : +3  分，1 天后
good（还行） : +12 分，2-4 天后（复习次数 <2 时 2 天，否则 4 天）
easy（熟练） : +20 分，5-10 天后（复习次数 <2 时 5 天，否则 10 天）
```

记忆度范围裁剪在 `[0, 100]`。这是有意选择的简化模型——用固定 delta 而非 SM-2 的 ease factor 动态调整，优点是易解释、易调试，缺点是不能按个体卡片的历史表现自适应调整间隔增长率。**这是当前算法的已知局限，下一步计划升级为真正的 SM-2**（引入 ease factor，根据连续评分动态调整间隔倍率），使其成为可以在面试中详细拆解的技术点。

### 6.3 知识图谱渲染

知识图谱**完全在客户端本地计算生成**，不调用 AI（虽然后端保留了 `/api/ai/knowledge-graph` 这个基于 AI 推断关系的接口，当前客户端未使用）。

- 数据源：直接复用 `/api/decks` 返回的卡组层级数据
- 每个根卡组渲染成一张独立的图，子卡组作为节点围绕根节点放射状排列
- 布局算法：自实现的 Fruchterman-Reingold 力导向算法（斥力+引力迭代 300 次收敛），在后台线程计算，避免阻塞 UI

## 7. AI 集成方式

与最初"用户自带 Key"的构想不同，当前版本采用**后端统一持有 Key**的模式：

- Key 配置在后端 `application.yml`，不下发到客户端
- 客户端只需要登录态（JWT），无需感知 AI 服务商细节
- 预留了 `customKey` 参数（用户可选传自己的 Key），但默认走系统 Key

选择这个模式的原因：产品面向的是"我要投简历用的作品"场景，统一管理 Key 更利于控制成本和调用频率，且更贴近多数中小型 SaaS 产品的真实做法（相比"让用户自己填 Key"更利于展示"网关代理 + 限流"这类后端能力，虽然限流还未实现，见下节）。

## 8. 已识别的待优化项（面试可深挖的方向）

按优先级：

1. **AI 生成接口异步化**：当前同步调用阻塞 Tomcat 工作线程最长 120 秒，高并发下会耗尽线程池。计划方案：独立线程池隔离（`ai-exec-N`），接口先返回 `pending` 状态和任务 ID，客户端轮询或推送结果
2. **N+1 查询优化**：`DeckServiceImpl.toVO()` 为每个卡组单独查一次卡片来算聚合统计（数量/平均记忆度），列表接口存在 N+1；计划改为一次 `GROUP BY` 聚合查询
3. **Redis 缓存**：今日计划、卡组列表等读多写少的数据可加缓存层，重点设计缓存失效策略（写操作后主动失效 vs. TTL）
4. **复习算法升级为 SM-2**：见 6.2 节
5. **并发安全**：`daily_plan.completed_count` 当前是"查询-修改-写回"的非原子操作，高并发下存在丢更新问题，计划改用数据库原子 `UPDATE ... SET completed_count = completed_count + 1`
6. **测试覆盖**：目前无单元测试，计划优先覆盖复习算法和权限校验这两块逻辑密集区域
7. **Docker Compose 一键启动**：简化本地开发与演示环境搭建

## 9. 后续可扩展方向

- 更细粒度的角色/权限体系（RBAC）
- 多设备同步（当前 iOS 单端）
- 社区卡包生态完善
- 模拟面试模式（基于选择题模式 `QuizModeView` 进一步扩展）
