# PrepDot 页面信息架构

> 本文档描述 iOS 客户端（SwiftUI，最低 iOS 26）**当前实际实现**的页面结构。

## 1. 页面结构总览

实际落地的信息架构比最初构想更收敛，核心是 **3 个常驻 Tab + 若干全屏功能页**，而非 6 个平级一级页面。这个调整基于一个判断：复习、卡组管理、个人中心这三件事使用频率差异巨大且互不干扰，适合用 TabView 常驻；而知识图谱、AI 生成、测验模式这些是"任务态"操作，更适合作为从卡组页触发的全屏沉浸页面，用完即关，不占用底部导航位。

```
TabView（底部 3 个常驻 Tab）
├─ 今日复习  ReviewView
├─ 我的卡组  DecksView
│    ├─ 知识图谱入口（卡片式，与卡组列表并列）→ 全屏 KnowledgeGraphView
│    ├─ AI 上传生成入口 → AIGenerateView（sheet）
│    ├─ 导入精选卡包入口 → CommunityView（sheet）
│    └─ 卡组详情 → DeckDetailView（可继续下钻子卡组，NavigationStack 保证可返回）
│         └─ 测验模式入口（叶卡组且卡片≥2张时出现）→ 全屏 QuizModeView
└─ 我的      ProfileTabView → ProfileView
     └─ 每日提醒设置 → NotificationSettingsView

未登录态：LoginView（登录/注册二合一，非 Tab 结构）
```

## 2. 导航层级设计

- **底部 TabView，3 个 Tab**，而非侧边导航：iOS 原生习惯，且页面数量少，不需要抽屉式导航
- **知识图谱从"弹窗"改为"全屏页面"**：早期版本用 `.sheet` 承载图谱，实际体验下来发现图谱是需要沉浸操作（拖拽、缩放、点击节点看详情）的内容，`.sheet` 的半高卡片形态和图谱的空间操作诉求冲突，改为 `.fullScreenCover` 全屏承载，入口从工具栏小图标升级为与卡组列表并列的大卡片（`GraphEntryCard`），提升可发现性
- **`DeckDetailView` 支持递归下钻**：AI 生成的卡组天然是层级结构（根卡组→子卡组），`NavigationLink(destination: DeckDetailView(deck: child))` 递归调用自身即可，无需为子卡组单独设计页面

## 3. 今日复习页（ReviewView）

### 3.1 页面目标

P0 页面，是全产品最核心的学习闭环入口：展示今日待复习卡片、提供翻卡评分的沉浸式复习流程。

### 3.2 页面模块

- **顶部统计条**（`StatPill`）：今日已复习数、连续打卡天数等，light/dark 模式分别适配文字颜色（早期版本浅色模式下白字在浅色背景上看不清，已修复）
- **闪卡主体**（`FlashcardView` + `CardFace`）：
  - 正面显示问题，`rotation3DEffect` 实现 3D 翻转动画
  - **背面同时保留问题（次要样式，灰色小字）+ 答案（主要样式，大字）**，而非翻面后问题完全消失——这是根据实际使用反馈调整的：复习时经常需要对照问题回看答案，完全隐藏问题会打断心流
  - 选择题类型（`card_type == "choice"`）背面额外展示选项列表，正确答案高亮
- **评分区**（`RatingButtons`）：again/hard/good/easy 四档，对应文案"重新学/明天/2-4天/5-10天"，让用户在点击前就能预期这次评分的后果
- **完成页**（`DoneView`）：复习完当日计划后展示，带极光背景动效

## 4. 我的卡组页（DecksView）

### 4.1 页面目标

P0 页面，卡组的管理中心：创建、AI 生成、导入、浏览、编辑、删除，以及知识图谱和测验模式的入口枢纽。

### 4.2 页面模块

- **iPad 适配**：使用 `NavigationSplitView`（而非普通 `NavigationStack`），左侧卡组列表 + 右侧详情列并排显示，详情列内部再包一层 `NavigationStack` 保证子卡组下钻后有返回按钮
- **知识图谱入口卡片**（`GraphEntryCard`）：列表顶部一个渐变图标 + "知识图谱 · N 个卡组"的说明卡片，与下方"我的卡组" Section 视觉并列，点击进入全屏图谱
- **卡组列表**（`DeckRow`）：每行展示标题、分类标签、卡片数/已复习数/记忆度三项统计、记忆度进度条；左滑编辑、右滑删除
- **顶部工具栏**：`+` 菜单聚合三个创建方式——手动新建 / AI 上传生成 / 导入精选卡包，避免三个平级按钮挤占空间
- **卡组详情页**（`DeckDetailView`）：
  - 有子卡组时显示子卡组列表（继续下钻）
  - 无子卡组时显示卡片列表，支持管理模式（批量勾选删除）
  - 工具栏"测验"入口：仅在叶卡组且卡片数 ≥2 时出现，进入 `QuizModeView` 全屏选择题自测

### 4.3 关联的全屏/弹出页面

| 页面 | 触发方式 | 说明 |
|---|---|---|
| `KnowledgeGraphView` | 全屏（`.fullScreenCover`） | 见第 5 节 |
| `AIGenerateView` | Sheet | 上传文件/粘贴文本 → AI 生成卡组预览 → 确认入库 |
| `CommunityView` | Sheet | 浏览精选卡包列表，一键导入 |
| `QuizModeView` | 全屏 | 见第 6 节 |
| `CreateDeckSheet` / `EditDeckSheet` | Sheet | 手动新建/编辑卡组的表单 |
| `CardFormSheet` | Sheet | 新建/编辑单张卡片，支持问答/选择题/填空三种类型切换 |

## 5. 知识图谱页（KnowledgeGraphView）

### 5.1 页面目标

P2 定位，产品亮点和辅助理解能力，把卡组的层级结构可视化。

### 5.2 与最初设想的差异

最初构想是"AI 推断卡片间关系"的语义图谱；实际实现改为**卡组结构图谱**——图谱节点是卡组层级关系（根卡组→子卡组），而非卡片间的语义关联。原因：
1. AI 语义关联的生成成本和不稳定性较高，MVP 阶段卡组层级本身已经是一种有意义的知识结构
2. 客户端本地计算即可完成渲染，不依赖额外的 AI 调用延迟

### 5.3 页面模块

- **顶部卡组选择器**：横向滚动的 pill 按钮，每个 pill 对应一个根卡组，切换时重新触发入场动画
- **画布区**：
  - 每次只渲染当前选中的一个根卡组的子图（每卡组独立成图，不同卡组之间无连线）——早期版本把所有卡组画在同一张力导向图里，视觉上纠缠成一团，改为"一次一图"后可读性大幅提升
  - 根节点居中，子卡组节点放射状均匀环绕，连线为直线（早期用贝塞尔曲线，视觉上偏"发散"，改直线更利于看清层级关系）
  - 支持拖拽平移、双指缩放
  - 节点大小/颜色随记忆度变化（浅粉→深紫，对应待加强→已掌握）
- **图例区**：右下角小尺寸图例（三色点+文字），早期版本图例框体过大遮挡内容，已收缩
- **详情面板**：点击节点后从底部推出（不是叠加覆盖，避免遮挡画布节点），展示节点归属、记忆度、关联子主题

## 6. 测验模式页（QuizModeView）

### 6.1 页面目标

复习页之外的第二种检验方式：把问答卡自动转换成选择题形式，四选项作答，适合快速自测。

### 6.2 页面模块

- 从卡组内已有卡片自动生成选择题（正确答案取自卡片本身，干扰项从同卡组其他卡片中抽取）
- 逐题作答 → 即时反馈正确/错误 → 结束后展示正确率统计
- 至少需要 2 张卡片才能开启（干扰项来源要求）

## 7. 我的页（ProfileView）

### 7.1 页面目标

P1 页面，个人学习数据概览 + 应用设置入口。

### 7.2 页面模块

- **头像区**：昵称首字母生成渐变头像，颜色可在设置中自定义
- **连续打卡环形进度**：基于今日已复习数 / 每日目标数计算
- **概览四格**（`MiniStatCard`）：卡组数、卡片数、总复习次数、平均记忆度
- **近 7 天复习柱状图**：`Charts` 框架渲染
- **记忆度分布条**：待加强/进行中/已掌握三段式色条 + 图例
- **设置区**：每日提醒设置（`NotificationSettingsView`，本地通知）、退出登录

## 8. 登录/注册页（LoginView）

未登录态展示，登录/注册二合一表单（`FloatingTextField` 自定义悬浮标签输入框），非 Tab 结构，登录成功后 `AuthManager` 状态变化触发 `ContentView` 切换到主 Tab 界面。

## 9. 页面跳转关系图

```
LoginView ──登录成功──▶ TabView(今日复习 / 我的卡组 / 我的)

今日复习 ──完成当日计划──▶ DoneView ──返回──▶ 我的卡组（引导继续学习）

我的卡组 ──点击知识图谱卡片──▶ KnowledgeGraphView（全屏）
我的卡组 ──点击 + → AI 上传生成──▶ AIGenerateView(sheet) ──确认──▶ 刷新卡组列表
我的卡组 ──点击卡组──▶ DeckDetailView ──有子卡组──▶ 递归下钻 DeckDetailView
                                    └─无子卡组──▶ 卡片列表 ──点击测验──▶ QuizModeView（全屏）

我的 ──点击提醒设置──▶ NotificationSettingsView
我的 ──退出登录──▶ LoginView
```

## 10. 页面优先级（当前状态）

### P0（已完成，核心闭环）
- 今日复习页
- 我的卡组页（含 AI 生成、卡片管理）

### P1（已完成）
- 我的页（数据概览 + 设置）
- 测验模式

### P2（已完成，持续打磨中）
- 知识图谱页——已完成三轮视觉迭代（弹窗→全屏、发散连线→直线层级、图例收缩），后续如需要可以升级为真正基于 AI 语义关系的图谱

## 11. 与最初设想的主要出入说明

这份文档相对最初版本做了实质性调整，原因在于：最初设想是桌面端本地优先应用的通用页面构架，实际交付是 iOS 原生客户端，两者在导航范式（侧边栏 vs. TabView）、任务态页面呈现方式（页面 vs. 全屏 modal）上有本质差异，因此没有沿用旧文档的页面清单做小修小补，而是按 iOS 实际实现重新梳理。
